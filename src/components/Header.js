import React from 'react'
import logo from '../img/logo.svg'

const Header = () => (
    <div className="header">
        <img 
            src={logo}
            alt="logo"
        />
    </div>
)

export default Header